# SỔ TRẠNG THÁI MẠCH (STATUS-LEDGER) — M1→M14

> Authority: `CLOSURE-CONTRACT.md` (D0–D8) + `CIRCUIT-FLOW-MAP.md` (bản đồ wiring).
> File này là **sổ trạng thái vận hành**: mỗi mạch ↔ suite chính ↔ kết quả D1 thật ↔ runtime probe ↔ closure record ↔ trạng thái.
> Ngày lập: 2026-09-10. Branch: `audit/runtime-guard-AUDIT-20260909`. HEAD lúc lập: `067e636216977c420e79811ebe4e75bd1c7bc05d` (HEAD **không** phải SHA pin của bất kỳ mạch nào ngoài M1).

## Quy tắc trạng thái (không được nới)

- `CLOSED` — chỉ khi có đủ **D0–D8** trên **cùng một SHA pin** + closure record trong `reports/circuit-closures/`.
- `CLOSED_WITH_KNOWN_GAP` — hợp lệ nhưng có ít nhất một mục mang `EVIDENCE_GAP` đã ghi rõ trong closure record. **Không** được đọc thành "đã xong hoàn hảo".
- `D1_PASS__NOT_CLOSED` — suite chính xanh nhưng **CHƯA** có pin/runtime/closure. **KHÔNG** được đọc là "xong"; đây mới là điều kiện cần, chưa đủ.
- `D1_FAIL` — suite chính đỏ (exit ≠ 0). Mạch chắc chắn chưa thể đóng.
- `NOT_VERIFIED` — chưa chạy. Không hàm ý hỏng, và cũng **không** được đọc thành "đã xong".
- `EVIDENCE_GAP` — đã chạy nhưng thiếu/không hợp lệ một mục bắt buộc.

## ⚠️ CẢNH BÁO VỀ BẰNG CHỨNG D1 (đọc trước khi dùng bảng)

1. **D1 inventory chạy trên working tree (dirty, chưa pin SHA).** Các suite dưới đây do AutoCoder chạy bằng `py -3.12 -m pytest <suite> -q --no-header` trên **working tree hiện tại**, nơi `git status` đang có nhiều file `.py` modified/untracked. Kết quả **không** gắn với một SHA bất biến.
2. **Đây là baseline để LẬP KẾ HOẠCH, không phải bằng chứng đóng mạch.** Mọi con số ở cột D1 chỉ trả lời câu hỏi "suite có đỏ không", **không** trả lời "mạch đã đóng chưa". Không được dùng bảng này để tuyên bố bất kỳ mạch M2–M14 nào là đã hoàn thành.
3. **M1 là ngoại lệ.** M1 có closure record + SHA pin riêng (`M01-closure.json`, sha_pin `765075312bdc55373a86d9c5577ac62140c7ad64`); kết quả D1 của M1 lấy từ `M01-evidence/D1-T01-pytest.txt`, **không** lấy từ `INVENTORY/`.
4. **`INVENTORY/*.txt` là output nguyên văn** của lần chạy đó (đã copy vào repo). Không có lần chạy lại nào trong lần lập sổ này.
5. **Exit code ≠ chất lượng test.** Cảnh báo `RequestsDependencyWarning` và traceback `Exception while exporting Span` (opentelemetry) xuất hiện trong nhiều file INVENTORY; chúng **không** làm đổi exit code của pytest, nhưng có mặt trong log và chưa được phân loại là vô hại.

## Bảng trạng thái 14 mạch

| Mạch | Tên | Suite chính | D1 (exit + tóm tắt) | Runtime probe (đã/chưa) | Closure record | Trạng thái | Bằng chứng |
|---|---|---|---|---|---|---|---|
| M1 | Boot & Background | `tests/T01_boot/` | exit 0 — 35 passed (7.78s, lần chạy re-pin) | **đã** (`GET /health` + `/readiness` + container log) | `M01-closure.json` (sha_pin `7650753…`) | **CLOSED_WITH_KNOWN_GAP** (D4 = EVIDENCE_GAP) | `M01-evidence/D1-T01-pytest.txt`, `M01-evidence/D2-docker.txt` |
| M2 | Ask & Chat | `tests/T02_contract/test_flow_02_ask_chat_scp_standard.py` | exit 0 — **34 passed** (21.33s, chạy lại tại pin `1f00d00…`, `M02-evidence/D1-T02-pytest.txt`; inventory cũ `INVENTORY/M2.txt` là 3F/30P **trước** khi fix MACH2 + contract 1a — đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=1f00d00…`: /health 200, /readiness ready, `service_identity.commit` == pin; /ask missing-context → FAIL/withheld/KILL fail-closed; /ask context-backed → PASS/UPHOLD + task COMPLETED và WS /chat → frame `verified` trên instance standard **tạm** :8001 cùng image, đã tắt) | `M02-closure.json` (sha_pin `1f00d00…`) | **CLOSED_WITH_KNOWN_GAP** (D3/D4/D5/D6/D7 = PASS_WITH_LIMITS — đọc known_gaps: chat route không đăng ký trên profile=core; PASS/verified chỉ chứng minh với answer source fixture local; D5 chưa có artifact review trong repo) | `M02-evidence/D1-T02-pytest.txt`, `D2-docker.txt`, `D3-*.json/.txt`, `D6-*.txt`, `D7-todo-scan.txt` |
| M3 | OpenAI-compat | `tests/T02_contract/test_flow_03_openai_compat_scp_standard.py` (+ `test_api_import_order_contract.py`) | exit 0 — **28 passed** (7.13s, chạy lại tại pin `3f29c18…`, `M03-evidence/D1-T03-pytest.txt`; repeat gộp import-order: 29 passed; regression flow_02: 34 passed; inventory cũ `INVENTORY/M3.txt` là 7F/21P **trước** khi fix — đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=3f29c18…`: /health 200, /readiness ready, `service_identity.commit` == pin; 11 probe HTTP thật trên instance **tạm** full-profile :8002 cùng image, đã teardown: no-auth → 401 auth-first; bad JSON/malformed messages → 400 envelope (fix M3, trước là 500); valid message → 200 envelope + verdict FAIL/governance KILL + withheld (fail-closed, không answer source); stream=true → 200 JSON đơn (SSE là gap); `/swe-bench/v1/chat/completions` → 200 shape; thiếu model → 422; `/chat/completions` top-level → 404) | `M03-closure.json` (sha_pin `3f29c18…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D6/D7 = PASS_WITH_LIMITS — đọc known_gaps: routes 404 trên deployment chính profile=core; không answer source nên endpoint luôn withheld; SSE chưa hỗ trợ; instance_id không persist) | `M03-evidence/D1-T03-pytest.txt`, `D2-docker.txt`, `D3-m3-runtime.json`, `D3-runtime-setup.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M4 | Control & Hands | `tests/T03_capability/test_flow_04_control_hands_scp_standard.py` (+ `tests/T03_capability/`) | exit 0 — **58 passed** (16.03s, chạy lại tại pin `e13fad4…`, `M04-evidence/D1-T04-pytest.txt`; repeat gộp regression: 76 passed; regression riêng token-PEP + token-boundary: 18 passed; inventory cũ `INVENTORY/M4.txt` là 7F/50P **trước** khi fix — đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=e13fad4…`: /health 200, /readiness ready, `service_identity.commit` == pin; 13/13 probe HTTP thật trên instance **tạm** full-profile :8003 cùng image, đã teardown: 6 negative no/wrong-token → 403 token-only fail-closed; PC token hợp lệ + không capability token → 403 CapabilityRequiredError tại cả /v3/pc/execute lẫn /v3/hands/execute; capability token mint trong container → /v3/hands/execute golden path 200 + kernel COMPLETED; /v3/pc/execute PEP pass-through — lệnh thật là platform-limit trong container Linux, execution thật chứng minh ở D1 Windows) | `M04-closure.json` (sha_pin `e13fad4…`) | **CLOSED_WITH_KNOWN_GAP** (D3/D4/D5/D6/D7 = PASS_WITH_LIMITS — đọc known_gaps: routes 404 trên deployment chính theo profile; lệnh /v3/pc/execute không chạy được trong container Linux; seal HIGH=0 là snapshot @1f00d00; 6 except:pass pre-existing có chủ đích; thiếu header/flow-map/runbook) | `M04-evidence/D1-T04-pytest.txt`, `D2-docker.txt`, `D3-m4-runtime.json`, `D3-runtime-setup.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M5 | Agent/Call | `tests/T03_capability/test_flow_05_agent_call_scp_standard.py` | exit 0 — **35 passed** (3.08s, chạy lại tại pin `6cdb424…`, `M05-evidence/D1-T05-pytest.txt`) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=6cdb424…`: /health 200, /readiness ready, `service_identity.commit` == pin; 8/8 probe HTTP thật trên instance **tạm** full-profile :8006 cùng image, đã teardown: 3 negative no-auth → 401 auth-first (verify_admin), 1 negative guard-fail → 403; 4 golden path 200: agent/status + agent/plan PLAN_READY + call/sessions (join token ngắn hạn redact) + agent/run dry-run; log scan 0 locked / 0 Traceback / 0 bypass) | `M05-closure.json` (sha_pin `6cdb424…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D6/D7 = PASS_WITH_LIMITS — đọc known_gaps: seal là snapshot @1f00d00; không reviewer độc lập riêng; golden path chỉ PLAN_READY offline chưa chứng minh EXECUTE thật với capability>0; WS signaling 2-peer chưa probe runtime; 6 instrumentation except:pass pre-existing đã phân loại) | `M05-evidence/D1-T05-pytest.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m5-probe.txt`, `D3-runtime-setup.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M6 | Prediction | `tests/T03_capability/test_flow_06_prediction_scp_standard.py` | exit 0 — **23 passed** (2.96s, chạy lại tại pin `75e994f…`, `M06-evidence/D1-T06-pytest.txt`, gộp regression `test_subsystem_prediction`; inventory cũ `INVENTORY/M6.txt` là 6F/12P **trước** khi fix — và cả 12 "passed" cũ đều là test `pass` trắng, đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=75e994f…`: /health 200 + commit == pin, /readiness ready; 14/14 probe HTTP thật trên instance **tạm** full-profile :8004 cùng image, đã teardown: 5 negative no-auth → 401; 5-fail → 429 lockout fail-closed tự hồi phục ~65s; run-cycle golden path 200 status ok + cycle stats + run/trace ledger; `/verify` 200 — trước fix luôn 500; limit 0 → 422; 2 run-cycle liên tiếp + 5 probe song song → 200 hết; 0 "database is locked" trong log probe lẫn main) | `M06-closure.json` (sha_pin `75e994f…`) | **CLOSED_WITH_KNOWN_GAP** (D3/D4/D5/D7 = PASS_WITH_LIMITS — đọc known_gaps: crawl internet thật chưa chứng minh (egress deny by design); seal 4a66b279 là snapshot @1f00d00; không có reviewer độc lập cho riêng diff M6; Learn-phase production-judge path dead; thiếu header/runbook) | `M06-evidence/D1-T06-pytest.txt`, `D2-docker.txt`, `D3-m6-runtime.json`, `D3-runtime-setup.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M7 | AutoFix & Policy | `tests/T03_capability/test_flow_07_autofix_scp_standard.py` | exit 0 — **43 passed** (46.59s, chạy lại tại pin `554f43e…`, `M07-evidence/D1-T07-pytest.txt`) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=554f43e…`: /health 200, /readiness ready, `service_identity.commit` == pin — lần build đầu dùng build-arg SHA gõ tay SAI bị chốt commit==pin phát hiện, đã build lại đúng; 8/8 probe HTTP thật trên instance **tạm** full-profile :8007 cùng image, đã teardown: 2 negative no-auth → 401; 6 golden 200: stats + permissions + attack-mode toggle true→false có state flip (singleton persist) + monitor + run-audit **observe mode** 200 với ast_scan chạy thật (5 findings, bắt fixture `scp/hello_bug.py`, 0 ghi source); log scan 0 locked / 0 Traceback) | `M07-closure.json` (sha_pin `554f43e…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D6/D7 = PASS_WITH_LIMITS — đọc known_gaps: seal là snapshot @1f00d00; không reviewer độc lập riêng; **apply mode chưa probe runtime** (chỉ observe); tier-3 human approval flow chưa probe; LLM fix path offline theo egress deny; 3 instrumentation except:pass pre-existing đã phân loại) | `M07-evidence/D1-T07-pytest.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m7-probe.txt`, `D3-container-log-tail.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M8 | Audit/Benchmark | `tests/T03_capability/test_flow_08_audit_benchmark_scp_standard.py` | exit 0 — **25 passed** (2.56s, chạy lại tại pin `c0f5f6f…`, `M08-evidence/D1-T08-pytest.txt`) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=c0f5f6f…`: /health 200, /readiness ready, `service_identity.commit` == pin; 9/9 probe HTTP thật trên instance **tạm** full-profile :8008 cùng image, đã teardown: 2 negative no-auth/internal-token → 401/403; golden: audit stats/findings 200 + batch benchmark chạy THẬT end-to-end PAUSED→QUEUED→COMPLETED có accounting per-item; adversarial: 2 SSRF target (metadata IP + internal host) → 400 fail-closed, unknown job → 404; log scan 0 locked / 0 Traceback) | `M08-closure.json` (sha_pin `c0f5f6f…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D7 = PASS_WITH_LIMITS — đọc known_gaps: seal là snapshot @1f00d00; không reviewer độc lập riêng; benchmark LLM thật chưa chứng minh (egress deny); audit findings rỗng trong container sạch; fitness_engine chỉ có bằng chứng D1) | `M08-evidence/D1-T08-pytest.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m8-probe.txt`, `D3-container-log-tail.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M9 | Threat & Counter | `tests/T03_capability/test_flow_09_threat_analysis_scp_standard.py` | exit 0 — **23 passed** (2.24s, chạy lại tại pin `b4134ac…` **sau fix**, `M09-evidence/D1-T09-pytest.txt`) + `reality_4-a-003` script **PASS 3/3 exit 0** (`D1-reality-4a003.txt`; trước fix FAIL — xem root_causes) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=b4134ac…`: /health 200, /readiness ready, `service_identity.commit` == pin; 8/8 probe HTTP thật trên instance **tạm** full-profile :8009 cùng image, đã teardown: 4 negative no-auth → 401 auth-first trên cả 4 endpoint qua `verify_admin` **trực tiếp** sau fix; 4 golden admin Bearer → 200 real stats + ledger; log scan 0 locked / 0 Traceback) | `M09-closure.json` (sha_pin `b4134ac…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D7 = PASS_WITH_LIMITS — đọc known_gaps: diff sau fix chưa deep scan lại; không reviewer độc lập riêng; crawler internet thật chưa chứng minh (egress deny); threat DB rỗng trong container sạch; **1 PRODUCT_FAIL được fix tại pin**: `check_admin` wrapper + MagicMock passthrough là test hook trong production auth path, đã thay bằng `Depends(verify_admin)` trực tiếp + de-mock THREAT-5 + sửa docstring path drift) | `M09-evidence/D1-T09-pytest.txt`, `D1-reality-4a003.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m9-probe.txt`, `D3-container-log-tail.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M10 | Streaming | `tests/T03_capability/test_flow_10_streaming_scp_standard.py` | exit 0 — **12 passed** (tại pin `1691f7f…`, `M10-evidence/D1-T10-pytest.txt`; deterministic: lần 1 x3 + re-run sau anomaly G9 đều 12/12; regression external_audit stream-contract 9 passed/2 skipped; inventory cũ `INVENTORY/M10.txt` là 5F/7P **trước** khi fix — và 6/7 "passed" cũ đều là test `pass` trắng, đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=1691f7f…`: /health 200, /readiness ready, `service_identity.commit` == pin; 12/12 probe HTTP thật **chạy 2 lần độc lập** trên instance **tạm** full-profile :8005 cùng image, đã teardown: 5 negative no/wrong/scheme-auth → 401 auth-first; 5-fail → 429 lockout fail-closed tự hồi phục ~65s, token hợp lệ cũng bị chặn trong lockout; golden path streaming thật qua httpx — 200 `text/event-stream; charset=utf-8`, 6 SSE frame với arrival timestamp tăng dần (data chảy thật qua chunked HTTP), final frame `step=final/status=complete`; 2 stream liên tiếp 200/200; 422 schema; burst song song 2 stream + health 200/200/200; log scan: 0 "database is locked", 0 bypass, grep stream_routes = 0, 3 Traceback đều là AutoFix M7-domain read-only pre-existing) | `M10-closure.json` (sha_pin `1691f7f…`) | **CLOSED_WITH_KNOWN_GAP** (D3/D4/D5/D7 = PASS_WITH_LIMITS — đọc known_gaps: route 404 trên deployment chính profile=core; step:error fault-injection không reach được nếu không mock judge; seal 4a66b279 là snapshot @1f00d00; không có reviewer độc lập cho riêng diff M10; multi-LLM crosscheck dead trên sync judge path (judge.py dùng chung, cross-circuit); thiếu header/runbook; **G9 anomaly mất evidence cuối session — toàn bộ evidence đã tái tạo bằng re-run thật**) | `M10-evidence/D1-T10-pytest.txt`, `D1-T10-regression.txt`, `D2-docker.txt`, `D3-m10-runtime.json`, `D3-runtime-setup.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M11 | Admin/Import | `tests/T03_capability/test_flow_11_admin_import_scp_standard.py` | exit 0 — **35 passed** (3.78s tại pin trước fix `4f5e1bf…`, `M11-evidence/D1-T11-pytest.txt`; **35 passed exit 0 lần 2 tại fix pin `8fc3560…`**, `D1-T11-pytest-postfix2.txt`) | **đã** (rebuild ×2 + force-recreate với `SCP_GIT_SHA=4f5e1bf…` rồi `8fc3560…`: /health 200, /readiness ready, `service_identity.commit` == pin; 8/8 probe HTTP thật trên instance **tạm** full-profile :8010, đã teardown: 3 negative no-auth → 401; golden: v98/v100 status 200 + antibodies/check chạy thật flagged=1 + import/jsonl **judge thật** — lần 1 lộ **PRODUCT_FAIL**: `v.verdict` attribute trên dict của judge → 100% question thành `errors` với HTTP 200 (fail-silently, cùng lớp bug dict-contract M10) → **fix `8fc3560`** → lần 2 verdict thật UNKNOWN + ESCALATE, errors=0; log scan 0 locked / 0 Traceback) | `M11-closure.json` (sha_pin `8fc3560…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D7 = PASS_WITH_LIMITS — đọc known_gaps: diff sau fix chưa deep scan lại; không reviewer độc lập riêng; **1 PRODUCT_FAIL được fix tại pin**; harness flow_11 (HEAD + WIP) pin MagicMock attribute-contract phát minh — file WIP của stream khác không được sửa, trung hòa bằng fallback attribute trong product, HANDOFF đổi mock sang dict-contract; D1 chạy trên dirty tree có WIP) | `M11-evidence/D1-T11-pytest.txt`, `D1-T11-pytest-postfix2.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m11-probe.txt`, `D3-m11-probe-postfix.txt`, `D3-container-log-tail.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M12 | Background WHY | `tests/T03_capability/test_flow_12_background_why_scp_standard.py` | exit 0 — **27 passed** (7.82s, chạy lại tại pin `fa9da62…`, `M12-evidence/D1-T12-pytest.txt`, deterministic x2 + regression flow_02 34P / flow_06 22P-0F; inventory cũ `INVENTORY/M12.txt` là 10F/16P **trước** khi fix — và 13/16 "passed" cũ là test `pass` trắng, đã lỗi thời) | **đã** (rebuild + force-recreate với `SCP_GIT_SHA=fa9da62…`: /health 200 + `service_identity.commit` == pin, /readiness ready; probe runtime trên deployment chính: `[DOUBT]` started + `doubt_ledger.jsonl` ghi report thật mỗi lần container start; WhyGate deterministic ALLOW + `why_gate_audit.jsonl` ghi; scheduled WHY verify loop chạy thật — cycle đầu sau đúng 180s warmup, log `[WHY-VERIFY] cycle: executed=1`, row flip `status=executed/verdict=PASS` với claim + executed_at; 0 "database is locked"; `D3-m12-runtime.json`) | `M12-closure.json` (sha_pin `fa9da62…`) | **CLOSED_WITH_KNOWN_GAP** (D3/D4/D5 = PASS_WITH_LIMITS — đọc known_gaps: why_sources không qua SCP_EGRESS_MODE (urllib riêng, gap egress chung); empty-ai_answer substring-PASS contract; seal 4a66b279 là snapshot @1f00d00; không reviewer độc lập riêng; live semantic sources qua internet chưa chứng minh; thiếu header/runbook) | `M12-evidence/D1-T12-pytest.txt`, `D1-T12-pytest-repeat.txt`, `D1-regression-flow02.txt`, `D1-regression-flow06.txt`, `D2-docker-build.txt`, `D2-docker-recreate.txt`, `D2-health.json`, `D3-m12-runtime.json`, `D3-gate-doubt-tails.txt`, `D6-ast-scan.txt`, `D7-todo-scan.txt` |
| M13 | Data sources & Learning | `tests/T03_capability/test_flow_13_free_api_learning_scp_standard.py` | exit 0 — **28 passed** (27 + 1 test pin `[LEARN-9]` sau fix `76b7624…`; `M13-evidence/D1-T13-pytest-postfix.txt`; 27P @8c7f522 của tiền nhiệm tái sử dụng hợp lệ — HEAD đầu session khớp) | **đã** (build `--build-arg SCP_GIT_SHA=76b7624…` + container ephemeral full-profile :8011: /health 200 + `service_identity.commit` == pin; probe P1–P6 + N3–N5: consolidate 200 `ok_stub_noop` sau fix — trước fix 500 vĩnh viễn = PRODUCT_FAIL đã fix; free-apis/search 200 catalog thật; learn/status+matrix 200; top-systems POST fetch GitHub+Wikipedia thật records=3; no-auth 401 toàn bộ; rate-limit 429 thật; `D3-m13-probe-postfix.txt` + `D3-m13-probe2.txt`) | `M13-closure.json` (sha_pin `76b7624…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5 = PASS_WITH_LIMITS — đọc known_gaps: egress deny KHÔNG enforce urllib; consolidator vẫn là minimal stub passthrough; main deployment core-profile không serve v104; seal snapshot @1f00d00; không reviewer độc lập riêng). **Cập nhật 2026-09-12 (EE/S13):** UPDATE EE: "EE egress enforcement shipped (commits 61d068f..eb25959): choke point safe_urlopen + 3 fetcher + static gate; V-EE verified runtime reversal (deny blocks example.com); 2 residual Session-bypass REMEDIATED by S13" (`91076a6` `_common.py::_http_get_json` requests-branch + `38cf2fc` `direct_api_verifier._session_get`, tests `4ffd15b` §(i) — đọc `reports/expert-panel/EE-egress-enforcement.md` § V-EE-remediation; egress suite 21P/2S declared skips, T03 777P/0F) | `M13-evidence/` |
| M14 | v106 Audit/Self-model | `tests/T03_capability/test_flow_17_self_model_capability_scp_standard.py` + `tests/T11_release/` | exit 0 — **3 passed** (flow_17 de-vacuous 1→3 tests sau fix `cf34778…`, 2 negative pin 401 moi) **và** exit 0 — **69 passed** (T11_release, khớp baseline) | **đã** (build `--build-arg SCP_GIT_SHA=cf34778…` + container ephemeral full-profile :8012: /health 200 + `service_identity.commit` == pin; probe 7/7: no-auth 401 cả 2 endpoint v106 sau fix — trước fix 200 NO-AUTH = BFLA PRODUCT_FAIL đã fix; admin 200 audit reports thật + capability matrix thật; unknown id graceful UNKNOWN/M0; M13 consolidate regression 200; `D3-m14-probe.txt`) | `M14-closure.json` (sha_pin `cf34778…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5 = PASS_WITH_LIMITS — đọc known_gaps: "Audit Engine" chỉ là report-listing stub, scp/audit_engine/ mất source chỉ còn __pycache__; capability maturity là static recompute tested_sha='HEAD'; main deployment core-profile không serve v106; seal snapshot @1f00d00; không reviewer độc lập riêng) | `M14-evidence/` |
| Track C1 (ngoài M1–M14 — kernel storage) | PostgreSQL KernelStorage (ADOPT-AND-FIX) | `tests/T04_kernel/test_pg_storage_parity.py` + `test_pg_migration.py` + `test_pg_storage_chaos.py` + `test_pg_boot_runtime.py` (cần `SCP_PG_TEST_DSN` = docker PG thật; thiếu env = declared infra-skip) | exit 0 — **17 passed** parity+migration trên PG thật (4.67s, `C1-evidence/pytest-pg-parity-migration.txt`) + **4 passed** chaos ×2 lần chạy độc lập (4.89s/4.85s, `pytest-pg-chaos.txt`) + **2 passed** boot-on-Pg lifecycle (`pytest-pg-boot-runtime.txt`); T04 FULL với PG: **200 passed** exit 0 (21.21s, `pytest-T04-full-final.txt`); T03 FULL: **709 passed** exit 0 (90.60s, `pytest-T03-final.txt`) | **đã** (container `scp-pg-test` postgres:16-alpine digest `sha256:cf78e766…`, PG 16.15, `D2-docker-pg-runtime.txt`; chaos injection thật: pg_terminate_backend giữa transaction → fail-loud + rollback sạch + journal valid; docker restart giữa claim → recovery LEASED→RECOVERING→re-claim được; 2-process claim race → đúng 1 lease; backup_to → pg_dump thật → restore DB mới → verify_integrity + projection khớp; boot lifecycle qua TaskKernel API thật: create→claim/lease→checkpoint→verify→commit COMPLETED + crash path HUMAN_REVIEW→QUEUED→COMPLETED) | `C1-postgres-closure.json` (sha_pin `72da6d3…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D7 = PASS_WITH_LIMITS/EVIDENCE_GAP — đọc known_gaps: performance chưa đo; multi-node/HA chưa; NOTIFY event bus = Track C2 riêng; seal snapshot @1f00d00 chưa quét lại commit C1; không reviewer độc lập riêng; SQLite-assumption census 2 chỗ ghi nhận chưa sửa) | `C1-evidence/` + `reports/expert-panel/C1-postgres-storage.md` |
| Track C2 (ngoài M1–M14 — event bus) | PgEventBus: bảng `scp_events` durable + NOTIFY wake-up (ADOPT-AND-FIX; NOTIFY chỉ là chuông, không phải queue) | `tests/T04_kernel/test_pg_event_bus.py` (cần `SCP_PG_TEST_DSN` = docker PG thật; thiếu env = declared infra-skip; test factory (i) chạy không cần PG) | exit 0 — **9 passed** ×2 lần chạy độc lập trên PG thật (3.52s/3.54s, `C2-evidence/pytest-pg-event-bus-run1.txt` + `run2.txt`): (a) publish→poll payload+thứ tự id; **(b) listener chết giữa 5 publish → replay_undelivered của instance mới đủ 5/5 đúng thứ tự — NOTIFY mất không mất event**; (c) fail×2→retry→delivered; (d) poison→dead=TRUE sau đúng max_attempts + CRITICAL log, channel unblocked; (e) NOTIFY wake <10s với poll_interval 60s; (f) fan-out 2 subscriber; (g) publish fail-closed; (h) schema objects; (i) factory opt-in fail-closed; T04 FULL với PG: **209 passed** exit 0 (25.18s, `pytest-T04-full-after-C2.txt`) | **đã** (container `scp-pg-test` postgres:16-alpine digest `sha256:cf78e766…` — cùng image C1, PG 16.15; LISTEN connection riêng qua `select.select`, NOTIFY roundtrip đo thật; delivery contract chạy end-to-end trên PG thật, no-mock) | `C2-eventbus-closure.json` (sha_pin `5b4a6da…`) | **CLOSED_WITH_KNOWN_GAP** (D4/D5/D7 = PASS_WITH_LIMITS/EVIDENCE_GAP — đọc known_gaps: CHƯA wire vào kernel events thực — chỉ infra + API opt-in; fan-out multi-node ack semantics chưa; performance/retention chưa đo; không chaos process-level; seal snapshot @1f00d00 chưa quét lại; không reviewer độc lập riêng; ~~CIRCUIT-FLOW-MAP chưa có hàng Track C2~~ — đã bổ sung hàng Track C1–C3 vào CIRCUIT-FLOW-MAP ngày 2026-09-12) | `C2-evidence/` + mục C2 trong `reports/expert-panel/C1-postgres-storage.md` |
| Track C3 (ngoài M1–M14 — sandbox evaluator) | SandboxEvaluator: autofix không tự chấm bài — pytest thật trên bản sao workspace trong system temp, fail-closed (ADOPT-AND-FIX Track C3 theo V2 P4; wire opt-in `SCP_SANDBOX_EVALUATOR=1`, env-off giữ nguyên behavior từng byte) | `tests/T04_kernel/test_sandbox_evaluator_e2e.py` (NO-MOCK, subprocess pytest thật; 1 case PG-gated cần PG thật, thiếu env = declared infra-skip) | exit 0 — **18 passed, 1 skipped** ×2 lần chạy (trước + sau hardening); case PG-gated `test_event_bus_eval_roundtrip_real_pg` **PASSED trên PG thật** (postgres:16-alpine :55432, schema per-test `scp_c3_<hex>`) — chi tiết 12 case ở `C3-sandbox-evaluator.md` §5.1; regression T03+T07: 730–731 passed, 2 failed **pre-existing tại base `e6acb56`** (đã xác minh bằng chạy lại isolated tại base) + 1 flaky order-dependent pre-existing (độc lập C3) — §5.3; Mimosa normal: HIGH=0 cho toàn bộ file C3, 4 HIGH còn lại pre-existing ở file C1/C2 không đụng — §5.4 | **đã (một phần)** — event-path smoke trên PG thật: publish `EVAL_REQUEST` self-contained → durable row → replay → handler evaluate pytest thật → `EVAL_RESULT`; loop service fail-closed exits (không env → exit 2; enabled thiếu DSN → exit 2) — §5.2; container compose `sandbox-evaluator` (profile `sandbox`) **chưa có probe runtime riêng** trong record | `reports/expert-panel/C3-sandbox-evaluator.md` (pin tại commit `45a7dc8`) — **LƯU Ý: đây là evidence report, KHÔNG phải closure JSON chuẩn** (không tồn tại `C3-*-closure.json`); lý do: C3 là **adoption track** ngoài hợp đồng D0–D8, đóng bằng verdict report + pin commit thay vì JSON schema D0–D8 | **PASS_WITHIN_SCOPE** (tại pin `45a7dc8`; KHÔNG đọc thành CLOSED/CLOSED_WITH_KNOWN_GAP; đọc known_gaps §6: subscribe-loop chưa wire vào worker loop; evaluate là single-process sandbox — isolated workspace evaluator, không phải hard sandbox namespace/cgroup; timeout theo semantics `subprocess.run`; scan Mimosa `completeness: partial`) | `reports/expert-panel/C3-sandbox-evaluator.md` (bằng chứng chính) + `tests/T04_kernel/test_sandbox_evaluator_e2e.py` (E2E 18 case) |

Tổng hợp theo exit code (inventory lập sổ 2026-09-10): **8 mạch xanh D1** (M1, M5, M7, M8, M9, M11, M13, M14) / **6 mạch đỏ D1** (M2, M3, M4, M6, M10, M12). **Cập nhật 2026-09-11:** M2 đã chạy lại tại pin `1f00d00…` sau chuỗi fix MACH2 + contract 1a → **34/34 exit 0** và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M02-closure.json`). **Cập nhật 2026-09-11 (buổi chiều):** M3 đã fix + chạy lại tại pin `3f29c18…` (28/28 flow_03 + import-order + M2 regression 34/34, exit 0) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M03-closure.json`; runtime probe 11/11 trên instance tạm cùng image pin). **Cập nhật 2026-09-11 (muộn):** M4 đã fix + chạy lại tại pin `e13fad4…` (58/58 flow_04 + 18/18 regression token-PEP/boundary, exit 0) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M04-closure.json`; runtime probe 13/13 trên instance tạm full-profile cùng image pin). **Cập nhật 2026-09-11 (tối):** M6 đã fix + chạy lại tại pin `75e994f…` (23/23 flow_06 + test_subsystem_prediction, exit 0) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M06-closure.json`; runtime probe 14/14 trên instance tạm full-profile cùng image pin; 5 PRODUCT_FAIL thật được fix: engine-wiring 503 vĩnh viễn, /verify luôn 500, KhamPha KeyError nuốt block, entity bị drop khỏi INSERT, SelfLearner learn no-op). Hiện **6/14 mạch có closure record** (M1, M2, M3, M4, M6, M10) — tất cả đều mang gap đã ghi rõ. **Cập nhật 2026-09-11 (đêm):** M10 đã fix + chạy lại tại pin `1691f7f…` (12/12 flow_10, exit 0, deterministic qua 4 lần chạy + regression external_audit stream-contract 9P/2S) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M10-closure.json`; runtime probe 12/12 chạy 2 lần độc lập trên instance tạm full-profile :8005 cùng image pin; 1 PRODUCT_FAIL thật được fix: `ask_stream` đọc verdict dạng attribute trong khi `RealityJudge.judge()` trả dict thuần → mọi stream chết ở `step:error` với HTTP 200, frame judge-done/final không bao giờ emit). Baseline cũ 5F/7P: 5 đỏ là HARNESS patch attribute không tồn tại (`stream_llm_response`, `LLMGateway`) hoặc patch `verify_admin` mà `Depends()` không quan sát; 6/7 "passed" là test `pass` trắng → de-vacuous theo FA-01 thành 12 test thật, 0 mock. Finding cross-circuit ghi nhận: sync judge path (`scp/runtime/judge.py:~117`) gọi `cross_verify` async không await → multi-LLM crosscheck dead, thuộc owner khác. **G9 anomaly:** bản evidence đầu tiên của M10 mất khỏi disk cuối session (nguyên nhân chưa xác minh) — toàn bộ evidence đã tái tạo bằng re-run thật, chi tiết trong `M10-closure.json` → known_gaps. **Cập nhật 2026-09-11 (đêm muộn, MC-GREEN-3 resume):** M13 đã fix + chạy lại tại pin `76b7624…` (28/28 flow_13 exit 0) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M13-closure.json`; runtime probe trên container ephemeral full-profile :8011 cùng image pin; 1 PRODUCT_FAIL thật được fix: `/v104/learn/consolidate` gọi method không tồn tại `consolidate_unverified()` → 500 vĩnh viễn — endpoint chưa bao giờ hoạt động; fix wire về API thật `consolidate([])` + payload trung thực `ok_stub_noop` vì consolidator là minimal stub passthrough). Falsify quan trọng: SCP_EGRESS_MODE=deny KHÔNG chặn urllib trong container (external fetch thật xảy ra) — mở rộng gap chung M12 G1; rate-limiter 429 hoạt động thật. Hiện **8/14 mạch có closure record** (M1, M2, M3, M4, M6, M10, M12, M13) — tất cả đều mang gap đã ghi rõ. **Cập nhật 2026-09-11 (đêm muộn, MC-GREEN-3 resume, tiếp):** M14 đã fix + chạy lại tại pin `cf34778…` (flow_17 3/3 de-vacuous + T11_release 69/69, exit 0) và có closure record → **CLOSED_WITH_KNOWN_GAP** (`M14-closure.json`; runtime probe 7/7 trên container ephemeral full-profile :8012 cùng image pin; 1 BFLA PRODUCT_FAIL thật được fix: cả 2 endpoint v106 tra 200 KHONG auth — harness flow_17 cu chinh la test pin contract mo, da de-vacuous 1→3 tests). Ghi nhan trung thuc: "Audit Engine" chi la report-listing stub (scp/audit_engine/ mat source), capability maturity la static recompute. Hiện **9/14 mạch có closure record** (M1, M2, M3, M4, M6, M10, M12, M13, M14) — tất cả đều mang gap đã ghi rõ. **Cập nhật 2026-09-12 (EE/S13):** UPDATE EE: "EE egress enforcement shipped (commits 61d068f..eb25959): choke point safe_urlopen + 3 fetcher + static gate; V-EE verified runtime reversal (deny blocks example.com); 2 residual Session-bypass REMEDIATED by S13" — cụ thể: `scp/core/question_fetchers/_common.py::_http_get_json` nhánh requests.Session bỏ qua SCP_EGRESS_MODE (V-EE-1, commit `91076a6`) và `scp/runtime/engine_parts/direct_api_verifier.py::_session_get` raw requests.Session wired vào /ask judge path không qua gate nào (V-EE-2, commit `38cf2fc`); cả 2 đều thêm `enforce_egress_policy(url)` fail-closed trước fetch, contract trả None/UNKNOWN giữ nguyên; tests section (i) `4ffd15b` (egress suite 21P/2S declared skips; T03 777P/0F — fail không tăng; ssrf_sweep_s2 + T05_gateway 73P).

**Suite xanh ≠ mạch đóng; phải có D0–D8.**

## KNOWN-RED (policy conflict, chờ owner quyết)

> **RESOLVED (policy A — commit `cb1c99e3c0c94818902836eb238d5813222d0518`, 2026-09-12):** T00 gate đã cài `tests/T00_integrity/declared_infra_skips.json` allowlist tường minh (7 file, env-guard token bắt buộc còn trong source, reason-pattern khớp; pytestmark/decorator không bao giờ allowlist). Contract tests 9 case fail-closed; T00 72/72 exit 0; T00+T04 277 passed/23 declared skips. Known-red này KHÔNG còn áp dụng.


> Pin ngày 2026-09-12 (Agent S11, V6 concern 1). Đã xác minh bằng chạy thật:
> `python -m pytest "tests/T00_integrity/test_meta_audit.py::test_meta_audit_no_skip_in_mandatory_tests" -q`
> → **1 failed** (AST scan bắt `pytest.skip` call thật trong mandatory test
> không có guard `platform.system`).

- **Test đỏ:** `tests/T00_integrity/test_meta_audit.py::test_meta_audit_no_skip_in_mandatory_tests`
  (T00 integrity gate — "mandatory tests must FAIL if blocked, unless OS-specific").
- **Nguyên nhân:** **7 test file** của Track C1/C2/C3/D1 dùng `pytest.skip`
  dạng **declared infra-skip** (skip khi thiếu hạ tầng ngoài môi trường: PG
  DSN `SCP_PG_TEST_DSN` không có / chromium-playwright chưa cài). Pattern skip
  này **đúng quy ước declared-infra-skip** đã dùng xuyên suốt C1/C2/C3/D1
  (message ghi rõ `declared infra-skip: …`), nhưng **xung đột trực diện với
  chính sách no-skip của T00 gate** — T00 quét AST toàn bộ `tests/T*/` và chỉ
  miễn trừ khi source chứa `platform.system`. 7 file:
  - `tests/T03_capability/test_playwright_backend.py` (D1 — chromium/playwright)
  - `tests/T04_kernel/test_pg_storage_parity.py` (C1 — `SCP_PG_TEST_DSN`)
  - `tests/T04_kernel/test_pg_migration.py` (C1 — `SCP_PG_TEST_DSN`)
  - `tests/T04_kernel/test_pg_storage_chaos.py` (C1 — `SCP_PG_TEST_DSN`)
  - `tests/T04_kernel/test_pg_boot_runtime.py` (C1 — `SCP_PG_TEST_DSN`)
  - `tests/T04_kernel/test_pg_event_bus.py` (C2 — `SCP_PG_TEST_DSN`)
  - `tests/T04_kernel/test_sandbox_evaluator_e2e.py` (C3 — 1 case PG-gated)
- **Trạng thái session này:** KHÔNG sửa test và KHÔNG sửa gate (cả hai đều có
  lý do riêng đúng: infra-skip là fail-loud có khai báo; T00 no-skip là ratchet
  chống skip lậu). Đỏ này là **policy conflict thật**, không phải test hỏng.
- **2 câu hỏi chờ owner quyết** (chỉ owner được phép chọn, agent không tự nới):
  - **(a)** Allowlist env-guard trong T00 gate: mở rộng ngoại lệ của
    `test_meta_audit_no_skip_in_mandatory_tests` cho skip call có điều kiện
    env (`SCP_PG_TEST_DSN`, playwright probe) — tức T00 nhận diện
    declared-infra-skip thay vì chỉ nhận diện `platform.system`.
  - **(b)** Giữ nguyên đỏ làm driver: T00 tiếp tục FAIL cho tới khi hạ tầng
    PG/chromium được coi là bắt buộc (CI cấp PG thật), dùng đỏ này làm lực
    đẩy hoàn thiện infra thay vì nới gate.

## Danh sách mạch CÒN LẠI phải hoàn thiện

### (a) `D1_FAIL` — phải sửa trước khi có thể nói tới đóng mạch (M2, M3, M4, M6, M10 ~~M12~~ — M12 đã đóng 2026-09-11)

Danh sách test FAILED trích nguyên từ `INVENTORY/*.txt`:

**M2 — Ask & Chat (3 failed)** — `INVENTORY/M2.txt`
> **UPDATE 2026-09-11 (đã xử lý):** 3 test đỏ này thuộc contract cũ; owner phê duyệt contract 1a (verified-frame), Agent T sửa file test theo hướng TĂNG strictness (thêm assert `governance==KILL`, precondition provider isolation, pin exact `provider_label`, bỏ legacy frame type `answer`); root cause thật là lỗ hổng isolation làm lộ OPENROUTER key slot 4-10 vào pytest (cloud round-trip). Suite chạy lại tại pin `1f00d00…`: **34/34 exit 0** (`M02-evidence/D1-T02-pytest.txt`). M2 đã có closure record `M02-closure.json` → **CLOSED_WITH_KNOWN_GAP**. Số 3 failed dưới đây là trích nguyên văn inventory CŨ (trước fix), giữ lại để audit lịch sử.
- `test_flow_02_ask_chat_scp_standard.py::TestFlow02AskEndpoint::test_ask_endpoint_llm_gateway_fallback_chain`
- `test_flow_02_ask_chat_scp_standard.py::TestFlow02WebSocketChat::test_ws_chat_message_exchange_fail_closed_without_llm`
- `test_flow_02_ask_chat_scp_standard.py::TestFlow02WebSocketChat::test_ws_chat_rate_limit_exceeded_closes_1008`

**M3 — OpenAI-compat (7 failed)** — `INVENTORY/M3.txt`
> **UPDATE 2026-09-11 (đã xử lý bởi Worker MC3):** 7 test đỏ flow_03 + 1 test `test_api_import_order_contract` (8 failure thuộc M3 theo ghi chú M2) đã được root-cause và đóng tại pin `3f29c18…` → **CLOSED_WITH_KNOWN_GAP** (`M03-closure.json`). Phân loại trung thực: 4 test patch thuộc tính KHÔNG TỒN TẠI (`LLMGateway`, `AgentOrchestrator`) hoặc phát minh contract (path top-level `/chat/completions`, 422-unauth, async pass-stub) → HARNESS sửa theo hướng TĂNG strictness + no-mock (golden path chạy judge thật); 2 PRODUCT_FAIL thật: malformed `messages`/JSON body crash 500 không cấu trúc → fix fail-closed 400/503 envelope; `v105_routes.py` thiếu import `Request` làm `app.openapi()` crash PydanticUserError → fix 1 dòng import; 1 stale pin v104 23→24 (route `/v104/learn/consolidate` thêm sau ngày pin). Số 7 failed dưới đây là trích nguyên văn inventory CŨ (trước fix), giữ lại để audit lịch sử. Chi tiết root-cause từng test: `M03-closure.json` → `root_causes`.
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_openai_chat_completions_validates_schema`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_openai_compat_handles_streaming_false`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_swe_bench_chat_completions_endpoint_exists`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_swe_bench_validates_instance_id`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_streaming_response_translation`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_openai_compat_gateway_failure_returns_503`
- `test_flow_03_openai_compat_scp_standard.py::TestFlow03OpenAICompat::test_swe_bench_compat_agent_failure_returns_error`

**M4 — Control & Hands (7 failed)** — `INVENTORY/M4.txt`
> **UPDATE 2026-09-11 (đã xử lý bởi Worker MC4, attempt 3):** 7 test đỏ đã root-cause và đóng tại pin `e13fad4…` → **CLOSED_WITH_KNOWN_GAP** (`M04-closure.json`). Phân loại trung thực: 0/7 là PRODUCT_FAIL trong số đỏ inventory — 6 HARNESS_BROKEN (authority dựng trên file state RỖNG → product fail-closed `state_corrupt` là đúng thiết kế; NameError `app`; giả định backup cho file mới; action `pc.execute` không thuộc registry + patch nhầm controller instance) và 1 PRODUCT_FAIL được fix bởi **fix kế thừa từ attempt 2** (bridge tạo kernel mutation TRƯỚC authorization — review, GIỮ, đánh verdict trong `M04-closure.json.root_causes.inherited_bridge_fix_attempt2`). Ngoài ra D3 runtime probe phát hiện 1 PRODUCT_FAIL MỚI ở HTTP boundary: `/v3/hands/execute` leak 500 thay vì 403 cho thiếu capability token → fix `e13fad4`. Thêm 1 test xanh-mơ-hồ (PLANNER-1) được de-vacuous theo FA-01. Mock helpers bị xoá toàn bộ khỏi module (suite mock-free). Số 7 failed dưới đây là trích nguyên văn inventory CŨ (trước fix), giữ lại để audit lịch sử. Chi tiết: `M04-closure.json` → `root_causes`.
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_pc_controller_execute_requires_token_and_capability`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_pc_controller_kill_clear_requires_capability_token`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_control_capability_status_requires_admin`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_control_capability_escalate_requires_admin`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_pc_controller_write_file_succeeds_with_valid_token`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_hands_executor_forwards_token_to_controller`
- `test_flow_04_control_hands_scp_standard.py::TestFlow04ControlHands::test_hands_executor_rejects_missing_token_fail_closed`

**M6 — Prediction (6 failed)** — `INVENTORY/M6.txt`
> **UPDATE 2026-09-11 (đã xử lý bởi Worker MC6):** 6 test đỏ đã root-cause và đóng tại pin `75e994f…` → **CLOSED_WITH_KNOWN_GAP** (`M06-closure.json`). Phân loại trung thực: 6/6 đỏ là HARNESS_BROKEN ở lớp path/mock (5 test đánh nhầm top-level path thay vì `/v105/predictions/*` đã đăng ký; PRED-6 mock golden path + sai response shape) — nhưng việc rewrite harness đã lộ **5 PRODUCT_FAIL thật** được fix tại pin: (1) `_get_engine()` đọc global `api_server` không bao giờ được gán → mọi endpoint prediction 503 vĩnh viễn; (2) `/verify` luôn 500 (`Verifier.verify_pending` không nhận `limit`); (3) KhamPha `spec["ai_answer"]` KeyError nuốt cả block; (4) `entity` bị chấp nhận nhưng không INSERT (mất dữ liệu im lặng) + migration cộng sung; (5) `SelfLearner` fallback không có `.classifier` → Learn phase là no-op im lặng. Nghi vấn audit cũ "M6 Lock DB": **không được xác nhận** trong phạm vi — WAL + busy_timeout 30s + RLock; PRED-7 8-thread×4 ghi thật không mất row; runtime probe 2 liên tiếp + 5 song song, 0 "database is locked". 12 test "passed" cũ đều là test trắng → de-vacuous theo FA-01 thành 22 test thật. Số 6 failed dưới đây là trích nguyên văn inventory CŨ (trước fix), giữ lại để audit lịch sử. Chi tiết: `M06-closure.json` → `root_causes`.
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_run_cycle_requires_admin`
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_pending_requires_admin`
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_all_requires_admin`
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_verify_requires_admin`
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_stats_requires_admin`
- `test_flow_06_prediction_scp_standard.py::TestFlow06Prediction::test_prediction_run_cycle_executes_v5_pipeline`

**M10 — Streaming (5 failed)** — `INVENTORY/M10.txt`
> **UPDATE 2026-09-11 (đã xử lý bởi Worker MC10):** 5 test đỏ đã root-cause và đóng tại pin `1691f7f…` → **CLOSED_WITH_KNOWN_GAP** (`M10-closure.json`). Phân loại trung thực: 5/5 đỏ là HARNESS_BROKEN — STREAM-2/3/6 patch `stream_llm_response`, STREAM-4 patch `LLMGateway` (cả hai KHÔNG TỒN TẠI trong stream_routes), STREAM-5 patch `verify_admin` mà `Depends(verify_admin)` đã capture lúc decorate nên patch không bao giờ được quan sát (401 auth-first là đúng). Việc rewrite harness no-mock đã lộ **1 PRODUCT_FAIL thật**: `ask_stream` đọc `verdict.verdict`… dạng attribute trong khi `RealityJudge.judge()` trả dict thuần → mọi stream kết thúc `step:error`, HTTP vẫn 200 (fail-silently) → fix dict-contract tại pin. 6/7 test "passed" cũ là `pass` trắng → de-vacuous (FA-01) thành 12 test thật, 0 mock, 0 skip/xfail. Số 5 failed dưới đây là trích nguyên văn inventory CŨ (trước fix), giữ lại để audit lịch sử. Chi tiết: `M10-closure.json` → `root_causes`.
- `test_flow_10_streaming_scp_standard.py::TestFlow10Streaming::test_stream_endpoint_accepts_streaming_request`
- `test_flow_10_streaming_scp_standard.py::TestFlow10Streaming::test_stream_sse_format_correct`
- `test_flow_10_streaming_scp_standard.py::TestFlow10Streaming::test_stream_handles_llm_gateway_failover`
- `test_flow_10_streaming_scp_standard.py::TestFlow10Streaming::test_stream_validates_request_schema`
- `test_flow_10_streaming_scp_standard.py::TestFlow10Streaming::test_stream_timeout_handling`

**M12 — Background WHY (10 failed)** — `INVENTORY/M12.txt`
> **UPDATE 2026-09-11 (đã xử lý bởi Worker MC12):** 10 test đỏ đã root-cause và đóng tại pin `fa9da62…` → **CLOSED_WITH_KNOWN_GAP** (`M12-closure.json`). Phân loại trung thực: 3 nhóm HARNESS_BROKEN phát minh contract (6 test DoubtCron dùng `db_path`/`max_questions_per_run`/`tick()` không tồn tại; 3 test WhyEngine gọi `verify_decision`/`lookup_sources`/`check_contradiction` không tồn tại; 1 test patch `open_meteo._fetch_weather` không tồn tại) và **6 PRODUCT_FAIL thật** được fix: (1) `init_why_db` dùng `logger` chưa định nghĩa → `WhyEngine()` chết NameError trên mọi DB mới; (2) `whyengine` không có `logger` → mọi error path NameError trong except; (3) `execute_plan` UPDATE…ORDER BY…LIMIT → OperationalError trên SQLite chuẩn → plan treo 'pending' vĩnh viễn; (4) bảng `why_verification_plans` thiếu cột `confidence_threshold` mà claim query RETURNING nó → executed=0 100%; (5) `VerificationPlan` chỉ tồn tại nhờ injection importlib theo thứ tự import → NameError runtime + nuốt claim; (6) `why_verify_loop` chỉ được wire trong `scp/api/_lifespan.py` (không phải lifespan active) + judge không có `why_engine` → background WHY loop không bao giờ chạy thật — đã wire vào `api_server_parts/lifespan.py` + attach engine vào judge, chứng minh runtime thật (cycle executed=1 sau đúng 180s). Thêm PF-7: `query_open_meteo` không encode target → mọi target nhiều từ chết. 13/16 test "passed" cũ là `pass` trắng → de-vacuous (FA-01) thành 27 test thật, 0 mock, network = local HTTP server thật qua seam `SCP_WHY_*_BASE`; test NASA cũ đi internet thật (NASA 500 làm inventory lệch 10F→11F). Chi tiết: `M12-closure.json` → `root_causes`.
- `test_flow_12_background_why_scp_standard.py::TestFlow12BackgroundWhy::test_doubt_cron_initializes_without_db_lock`
- `…::test_doubt_cron_runs_checks`
- `…::test_doubt_cron_persists_report_to_jsonl`
- `…::test_doubt_cron_respects_interval`
- `…::test_doubt_cron_handles_check_errors_silently`
- `…::test_doubt_cron_stops_background_thread`
- `…::test_why_engine_verifies_past_decisions`
- `…::test_why_engine_looks_up_external_sources`
- `…::test_why_engine_detects_contradictions`
- `…::test_query_open_meteo_returns_results`

**Ràng buộc sửa (theo `AGENTS.md` + DNA):** sửa **PRODUCT/HARNESS tại điểm lỗi**, không làm xanh bằng delete/skip/xfail/hạ assertion. Mỗi nhóm sửa phải kèm repro + xác nhận root cause trước khi sửa (DNA #1, #26). `INVENTORY/M12.txt` có ít nhất một `TypeError` tại `tests/T03_capability/test_flow_12_background_why_scp_standard.py:42` — cần đọc kỹ để phân biệt lỗi harness với lỗi product trước khi kết luận.

### (b) `D1_PASS__NOT_CLOSED` — suite xanh nhưng **chưa đóng mạch** (M5, M7, M8, M9, M11, M13, M14)

Các mạch này **chưa có gì ngoài D1**. Việc còn lại, theo `CLOSURE-CONTRACT.md`:

| Mạch | D0 khóa phạm vi + SHA pin | D2 runtime proof | D3 adversarial | D4 ratchet | D5 review độc lập | D6 fail-loudly | D7 wiring/docs | D8 closure record |
|---|---|---|---|---|---|---|---|---|
| ~~M5 Agent/Call~~ | ✅ `6cdb424…` | ✅ | ✅ 8/8 | ⚠️ seal-only | ⚠️ | ⚠️ 9 except:pass đã phân loại | ⚠️ | ✅ `M05-closure.json` |
| M7 AutoFix & Policy | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| M8 Audit/Benchmark | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| M9 Threat & Counter | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| M11 Admin/Import | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| ~~M13 Data sources & Learning~~ | ✅ `76b7624…` | ✅ ephemeral full-profile :8011 | ✅ P1–P6 + N3–N5 (1 PRODUCT_FAIL fix tại pin) | ⚠️ seal-only | ⚠️ | ✅ 0 silent / 20 handlers | ✅ 0 TODO | ✅ `M13-closure.json` |
| ~~M14 v106 Audit/Self-model~~ | ✅ `cf34778…` | ✅ ephemeral full-profile :8012 | ✅ 7/7 (1 BFLA PRODUCT_FAIL fix tại pin) | ⚠️ seal-only | ⚠️ | ✅ 0 silent / 2 handlers | ✅ 0 TODO | ✅ `M14-closure.json` |

> **Cập nhật 2026-09-11 (đêm, MC-GREEN):** M5, M7, M8, M9 đã đóng → dòng của chúng
> trong bảng trên được đánh dấu hoàn thành tương ứng (`~~gạch ngang~~` + ✅);
> M7/M8/M9-closure.json chi tiết hơn bảng này (bảng chỉ là tóm tắt). M9 có 1
> PRODUCT_FAIL được fix tại pin `b4134ac…` (check_admin MagicMock hook).

(D1 của nhóm này cũng **chưa** đủ điều kiện làm D1 hợp lệ: D1 yêu cầu "suite chính exit 0 + lưu toàn bộ output làm artifact **trên SHA pin**"; output hiện có là chạy trên working tree dirty.)

### (c) M1 — đã có closure, còn nợ đã ghi rõ

M1 **không** nằm trong danh sách "còn lại" ở dạng mở, nhưng **không được đọc là sạch**: `M01-closure.json` ghi `falsification_status = PARTIALLY_FALSIFIED_AT_PIN`, `D4_ratchet = EVIDENCE_GAP` (hook ledger `runStatus=inconclusive`, `scanned_files=0`, 6/6 file phạm vi `scanner_failed`; mốc "HIGH toàn dự án" chưa từng được đo đầy đủ). D5 của PIN2 **đã có 2 bản review độc lập** (`M01-evidence/D5-reviewer-3-pin2.md` = APPROVE_WITH_LIMITS, `D5-reviewer-4-overclaim.md` = PARTIAL/không thấy bịa đặt) nên nhãn D5 nay là `PASS_WITH_LIMITS`, **không** phải PASS trơn; còn nợ: `reviewer_limits` L1 (HEAD ≠ sha_pin → vi phạm điều khoản D8 "cùng commit"), L3 (evidence D1/D3 chạy ở parent, chỉ content-equivalent), L5 (header 4 module ghi `STATUS: CLOSED`). Chi tiết: `M01-closure.json` → `reviewer_limits` + `known_gaps`.

## Thứ tự đề xuất

Nguyên tắc xếp thứ tự: (1) sửa theo **cụm cùng root cause** để một lần điều tra trả nợ nhiều mạch; (2) nhóm đỏ trước nhóm xanh, vì mạch đỏ chặn cả D1; (3) việc đóng closure (D0/D2/D3/D4/D5/D8) làm sau, khi D1 đã ổn định và có pin.

1. **Cụm authz/admin — M4 + M6.** Cả hai đỏ gần như đồng dạng quanh kiểm quyền: M6 là 5/6 test `*_requires_admin` + 1 test pipeline; M4 là token/capability PEP + `*_requires_admin`. Cùng một lớp nguyên nhân (ranh giới token/admin) → sửa một lần, hồi quy cả hai.
2. **Cụm gateway/chat — M2 + M3 + M10.** M2 (`llm_gateway_fallback_chain`, WS fail-closed, WS rate-limit 1008), M3 (`gateway_failure_returns_503`, streaming translation), M10 (SSE format, gateway failover, timeout) đều là một trục: hành vi fail-closed/fail-over và hợp đồng streaming của gateway. Sửa trục này trước khi đóng bất kỳ mạch nào ở nhóm xanh liên quan tới LLM.
3. **Cụm WHY — M12.** 10 test đỏ tập trung vào `doubt_cron` + `why_engine` + `open_meteo`; có `TypeError` tại dòng 42 nên cần phân loại harness-vs-product trước.
4. **Đóng closure cho nhóm D1_PASS.** Sau khi D1 ổn định: chốt SHA pin → D0 → D2 runtime probe thật → D3 adversarial → D4 ratchet → D5 review độc lập → D6 → D7 → D8. Đề xuất thứ tự trong nhóm: M5, M8, M9, M11, M13 (suite nhỏ, 1 file) trước; M7 và M14 sau (M14 cần gộp 2 suite: `flow_17` + `tests/T11_release/`).
5. **Đóng nợ M1.** Chạy lại D4 đúng phạm vi M1 để đóng `EVIDENCE_GAP`; D5 của PIN2 nay đã có 2 bản review độc lập (`D5-reviewer-3-pin2.md`, `D5-reviewer-4-overclaim.md`) — việc còn lại là `reviewer_limits` L1/L3/L5 (khep D8 "cùng commit" đòi re-pin theo đúng trình tự, evidence D1/D2/D3/D4 phải chạy lại trên checkout sạch của SHA pin, và header 4 module về `CLOSED_WITH_KNOWN_GAP`). Không đóng được thì giữ nguyên `CLOSED_WITH_KNOWN_GAP` — tuyệt đối không hạ chuẩn D4 để lấy `CLOSED`.

**Suite xanh ≠ mạch đóng; phải có D0–D8.**

## Giới hạn bằng chứng của sổ này (missing pieces)

- Không có SHA pin cho D1 của M2–M14 → **không thể** dùng số liệu này để chứng minh bất kỳ điều gì về một commit cụ thể. Working tree đang dirty; kết quả có thể lệch nếu chạy lại sau khi có người sửa `.py`.
- Không có runtime probe nào cho M2–M14 trong lần lập sổ → cột "Runtime probe" = "chưa" là **thiếu bằng chứng**, không phải "đã kiểm và không có vấn đề".
- Sổ này **không** phân loại nguyên nhân gốc của 38 test đỏ (3+7+7+6+5+10). Danh sách FAILED là trích nguyên văn, chưa phải chẩn đoán.
- Số lượng test xanh/đỏ **không** nói gì về coverage, mutation score, hay chất lượng assertion.
- M14 gộp 2 suite trong một mạch; `INVENTORY/M14a.txt` (1 passed) + `M14b.txt` (69 passed) đều xanh, nhưng chưa xác minh được `tests/T11_release/` có thực sự phủ đủ phạm vi D1 của M14 theo `CLOSURE-CONTRACT.md` hay không.
- Cảnh báo dependency (`urllib3`/`chardet`) và lỗi export span OpenTelemetry xuất hiện trong log INVENTORY nhưng chưa được đánh giá là vô hại hay có hại cho kết quả.
- Sổ này **không** tuyên bố bất kỳ mạch nào "an toàn", "production-ready", hay "không còn vấn đề". `PASS` chỉ nghĩa là chưa quan sát thấy lỗi trong phạm vi đã chạy.
