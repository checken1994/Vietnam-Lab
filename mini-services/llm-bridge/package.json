{
  "name": "scp-llm-bridge",
  "version": "1.2.0",
  "description": "Ollama-compatible SCP bridge.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "build": "bun build index.ts --target=bun --outdir dist"
  },
  "dependencies": {},
  "engines": {
    "bun": ">=1.3.0"
  }
}
