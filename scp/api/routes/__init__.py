"""
[Task 7-A] API route modules — extracted from api_server.py

TẠI SAO: api_server.py 2,285 LOC with 64 routes. Refactor: tách thành
route modules per version. Backward-compatible — public API unchanged.

Modules:
  admin_v98.py — /v98/* security endpoints (8 routes)
  admin_v100.py — /v100/* knowledge + antibody + h8 endpoints (11 routes)
"""
