schema_version: 1
reference_id: complete-scp
reference_version: 1.0.0
bindings:
  epistemic.ontology:
    implementations:
    - scp.knowledge.ontology
    required_now: true
  epistemic.evidence:
    implementations:
    - scp.epistemic.evidence_store
    - scp.epistemic.evidence_writer
    required_now: true
  epistemic.lineage:
    implementations:
    - scp.epistemic.source_identity
    - scp.epistemic.lineage
    required_now: true
  epistemic.calibration:
    implementations:
    - scp.calibration.ledger
    - scp.calibration.legacy_adapter
    required_now: true
  self.capability_map:
    implementations:
    - scp.self_model.capability_map
    required_now: true
  governance.drift_guard:
    implementations:
    - scp.governance.drift_guard
    required_now: true
  governance.privacy_retention:
    implementations:
    - scp.governance.privacy
    - scp.governance.retention
    required_now: true
  cognitive.promotion_authority:
    implementations:
    - scp.knowledge.promotion_authority
    required_now: true
  cognitive.doubt_authority:
    implementations:
    - scp.knowledge.open_question_authority
    - scp.knowledge.contradiction_authority
    required_now: true
  cognitive.revalidation_authority:
    implementations:
    - scp.knowledge.revalidation_authority
    required_now: true
  cognitive.experiment_authority:
    implementations:
    - scp.knowledge.experiment_authority
    required_now: true
